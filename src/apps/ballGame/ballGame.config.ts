import {AppConfig} from "@/types/App"

const ballGameConfig: AppConfig = {
  name: "ballGame",
  desktopName: "弹球",
  iconName: "icon--game",
  windowMode: {
    mode: "FLOAT",
    appHead: true,
    width: "600px",
    height: "800px",
    left: "200px",
    top: "100px"
  },
}

export default ballGameConfig
