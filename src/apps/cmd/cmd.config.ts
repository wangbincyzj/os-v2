import {AppConfig} from "@/types/App"

const cmdConfig: AppConfig = {
  name: "cmd",
  iconName: "icon-cloudshellyunminglinghang",
  windowMode: {
    mode: "FIXED",
    left: "0",
    bottom: "0",
    width: "100%",
    height: ""
  }
}

export default cmdConfig
